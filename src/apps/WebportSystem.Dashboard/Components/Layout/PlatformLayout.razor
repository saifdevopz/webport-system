@using WebportSystem.Dashboard.Components.Pages.Index
@inherits LayoutComponentBase
@attribute [Authorize(Policy = "PlatformOnly")]

<MudThemeProvider Theme="ThemeConfig.CurrentTheme" />
<MudPopoverProvider />
<MudDialogProvider FullWidth="true"
                   MaxWidth="MaxWidth.ExtraSmall"
                   CloseButton="true"
                   BackdropClick="false"
                   Position="DialogPosition.Center"
                   CloseOnEscapeKey="true" />

<AuthorizeView Policy="PlatformOnly">
    <Authorized>
        <MudLayout>
            <MudAppBar Elevation="1">
                <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
                <MudSpacer />
                <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" />
            </MudAppBar>
            <MudDrawer @bind-Open="_drawerOpen" Elevation="2">
                <MudDrawerHeader>
                    <MudText Typo="Typo.h5" Class="mt-1">Application</MudText>
                </MudDrawerHeader>
                @*NavMenu*@
                <MudPaper Width="250px" Class="d-inline-flex py-3" Elevation="0">
                    <MudNavMenu Class="mud-width-full">
                        <MudText Typo="Typo.h6" Class="px-4">@_TenantContext.Role</MudText>
                        <MudText Typo="Typo.body2" Class="px-4 mud-text-secondary">@_TenantContext.Email</MudText>
                        <MudDivider Class="my-2" />
                        <MudNavLink Href="/platform/dashboard" Icon="@Icons.Material.Filled.Dashboard">Dashboard</MudNavLink>
                        <MudNavGroup Title="Identity" Icon="@Icons.Material.Filled.PermIdentity" Expanded="false">
                            <MudNavLink Href="/" Icon="@Icons.Material.Filled.DeleteForever">Tenants</MudNavLink>
                        </MudNavGroup>
                        <MudNavLink OnClick="LogoutClicked" Icon="@Icons.Material.Filled.Logout">Logout</MudNavLink>                        
                    </MudNavMenu>
                </MudPaper>
            </MudDrawer>
            <MudMainContent>
                <ErrorBoundary @ref="errorBoundary">
                    <ChildContent>
                        @Body
                    </ChildContent>

                    <ErrorContent Context="exception">
                        <p>Platform Layout - Error Boundary</p>
                        <p>@exception.GetType()</p>
                        <p>@exception.Message</p>
                        <p>@exception.StackTrace</p>
                        <button @onclick="() => errorBoundary?.Recover()">Try Again</button>
                    </ErrorContent>
                </ErrorBoundary>                
            </MudMainContent>
        </MudLayout>
    </Authorized>
    <NotAuthorized>
        <IndexPage />
    </NotAuthorized>
</AuthorizeView>

@code{
	private ErrorBoundary? errorBoundary;

    protected override void OnParametersSet()
    {
        errorBoundary?.Recover();
    }

    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
    private void LogoutClicked()
    {
        _NavigationManager.NavigateTo("/logout", forceLoad: true);
    }

}